# Deprecated: use `.github/workflows/ci.yml` (includes deploy on main).
# Kept for reference — remove after confirming GitHub Actions secrets are configured.

name: Deploy VPS

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Deploy to VPS
        uses: appleboy/ssh-action@v1.2.0

        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_SSH_KEY }}
          port: ${{ secrets.VPS_PORT }}

          script: |
            cd /var/www/ntbhub-backend

            git pull origin main

            npm install

            npx prisma generate

            npx prisma migrate deploy

            npm run build

            pm2 restart ntbhub-api
